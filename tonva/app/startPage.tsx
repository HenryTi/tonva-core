import { AppConfig, CAppBase } from './CAppBase';
/*
import { nav } from '../components';

export async function startPage<E>(CApp: new (config: AppConfig) => CAppBase<E, any>, appConfig: AppConfig) {
	nav.setSettings(appConfig);

	let cApp = new CApp(appConfig);
	cApp.internalInit();
    await cApp.start();
}
*/